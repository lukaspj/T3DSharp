        public static {{function.ReturnType.ManagedType}} {{function.Name}}({% for arg in function.Args %}{{arg.Type.ManagedType}} {{arg.Name}}{% if forloop.rindex0 != 0 %}, {% endif %}{% endfor %}) {
{% if function.IsVariadic -%}
{% else -%}
            {% getReturnString function.ReturnType %}InternalUnsafeMethods.{{function.Name}}({% for arg in function.Args %}{% marshal arg %}{% if forloop.rindex0 != 0%}, {% endif %}{% endfor %}){% endgetReturnString %};
{% endif -%}
        }